#include <msp430.h>
void main(void) {

    WDTCTL = WDTPW + WDTHOLD;

    P1DIR = BIT0;
    P1REN = BIT3;
    P1OUT = BIT3;

    while(1){

        if((P1IN & BIT3)!=BIT3){
        _delay_cycles(250000);
        P1OUT ^= BIT0;
        }
    }
}
