#include <msp430.h>
void main(void) {

    WDTCTL = WDTPW + WDTHOLD;

    P1DIR = BIT0;
    P1REN = BIT1;
    P1OUT = BIT1;

    while(1){

        if((P1IN & BIT1)!=BIT1){
        _delay_cycles(250000);
        P1OUT ^= BIT0;
        }
    }
}
